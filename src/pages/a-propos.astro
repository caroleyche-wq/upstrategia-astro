---
import Layout from "../layouts/Layout.astro";

const geo = { city: "Rennes", region: "Bretagne", country: "France" };

const principles = [
  {
    title: "On commence par l’essentiel",
    text: "Suivi, relances, base de contacts : tant que les bases ne tiennent pas, le reste ne tient pas non plus.",
  },
  {
    title: "Simple, mais carré",
    text: "Un CRM simple, des prochaines actions visibles, et une routine réaliste. Pas d’usine à gaz.",
  },
  {
    title: "Zéro jargon",
    text: "On parle en termes concrets : qui relancer, quand, pourquoi, et avec quel message.",
  },
  {
    title: "Automatiser seulement si ça aide vraiment",
    text: "Rappels, relances, petites automatisations : uniquement si c’est utile, maîtrisé et mesurable.",
  },
];

const who = {
  for: [
    "Dirigeants de TPE / PME B2B qui manquent de temps",
    "Entreprises avec des contacts éparpillés (Excel, mails, outils différents)",
    "Structures qui veulent un suivi commercial clair (sans outil compliqué)",
  ],
  notFor: [
    "Les “promesses miracle” ou les méthodes copiées-collées",
    "Les projets sans objectif business (contact, RDV, relances, pipeline)",
    "L’automatisation à tout prix (sans base solide)",
  ],
};

const steps = [
  {
    title: "1) On clarifie",
    text: "Votre situation, vos priorités, et ce qui bloque vraiment (suivi, relances, base, messages).",
  },
  {
    title: "2) On structure",
    text: "Un minimum efficace : pipeline lisible, prochaines actions, et un système qui tient au quotidien.",
  },
  {
    title: "3) On active",
    text: "Mise en place (CRM simple / relances / base) + messages courts prêts à l’emploi.",
  },
  {
    title: "4) On stabilise",
    text: "Documentation + routine : pour que ça continue même quand vous êtes débordé(e).",
  },
];

const SITE = "https://www.upstrategia.fr";
const aboutJsonLd = {
  "@context": "https://schema.org",
  "@type": "AboutPage",
  name: "À propos - UpStrategia",
  url: `${SITE}/a-propos`,
  about: {
    "@type": "Organization",
    name: "UpStrategia",
    url: SITE,
    areaServed: "FR",
  },
  address: {
    "@type": "PostalAddress",
    addressLocality: geo.city,
    addressRegion: geo.region,
    addressCountry: geo.country,
  },
};
---

<Layout
  title={`À propos – UpStrategia : CRM simple, relances & base de données – ${geo.city}`}
  description={`UpStrategia : une approche simple pour dirigeants de TPE/PME B2B. CRM simple, relances propres et base clients/prospects exploitable. Basée à ${geo.city} (${geo.region}), visio ${geo.country}.`}
  canonical="/a-propos"
>
  {aboutJsonLd && (
    <script type="application/ld+json" set:html={JSON.stringify(aboutJsonLd)} />
  )}

  <main class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">

    <!-- HERO -->
    <section class="py-12 sm:py-16">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-8 sm:p-12">
        <div class="grid gap-10 lg:grid-cols-[1.1fr_.9fr] lg:items-center">

          <div>
            <p class="text-xs uppercase tracking-wide text-[var(--text-muted)]">
              À propos — {geo.city} / {geo.region} — visio {geo.country}
            </p>

            <h1 class="mt-2 text-3xl sm:text-5xl font-semibold tracking-tight text-[var(--brand-blue)]">
              Une approche simple et structurée pour ne plus perdre d’opportunités
            </h1>

            <p class="mt-4 text-base sm:text-lg text-[var(--text-muted)]">
              J’aide les dirigeants de <strong>TPE / PME B2B</strong> à remettre de l’ordre dans leur suivi commercial :
              <strong> CRM simple</strong>, <strong>relances propres</strong>, <strong>base de contacts exploitable</strong>.
              Sans jargon, sans outil compliqué, avec une mise en place réaliste.
            </p>

            <div class="mt-7 flex flex-wrap gap-3">
              <a
                href="/contact?from=a-propos&diagnostic=1"
                class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
              >
                Faire le point (15 min)
              </a>
              <a
                href="/offres"
                class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-white px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
              >
                Voir les offres
              </a>
            </div>

            <p class="mt-4 text-xs text-[var(--text-muted)]">
              Réponse sous 48h (en général). Basée à {geo.city}.
            </p>
          </div>

          <!-- PHOTO -->
          <aside class="rounded-3xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
            <div class="aspect-[4/5] overflow-hidden rounded-2xl bg-black/5">
              <img
                src="/images/carole-yche.PNG"
                alt="Carole Yché - consultante UpStrategia"
                class="h-full w-full object-cover"
                loading="lazy"
              />
            </div>

            <div class="mt-5">
              <p class="text-sm font-semibold text-[var(--brand-blue)]">Carole Yché</p>
              <p class="mt-1 text-sm text-[var(--text-muted)]">
                Marketing B2B & structuration du suivi commercial (CRM, relances, base de données).
              </p>
            </div>
          </aside>

        </div>
      </div>
    </section>

    <!-- POUR QUI / PAS POUR QUI -->
    <section class="py-12 sm:py-16">
      <div class="grid gap-4 lg:grid-cols-2">
        <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
          <h2 class="text-xl font-semibold text-[var(--brand-blue)]">Pour qui ?</h2>
          <ul class="mt-4 space-y-2 text-sm text-[var(--text-muted)]">
            {who.for.map((x) => (
              <li class="flex gap-3">
                <span class="mt-2 h-1.5 w-1.5 rounded-full bg-[var(--brand-copper)]"></span>
                <span>{x}</span>
              </li>
            ))}
          </ul>
        </div>

        <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
          <h2 class="text-xl font-semibold text-[var(--brand-blue)]">Pas adapté si…</h2>
          <ul class="mt-4 space-y-2 text-sm text-[var(--text-muted)]">
            {who.notFor.map((x) => (
              <li class="flex gap-3">
                <span class="mt-2 h-1.5 w-1.5 rounded-full bg-[var(--brand-copper)]"></span>
                <span>{x}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    <!-- POURQUOI / PRINCIPE -->
    <section class="py-12 sm:py-16">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Pourquoi cette approche ?</h2>
      <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
        J’ai vu le même scénario revenir souvent : beaucoup d’efforts, mais un suivi commercial flou.
        Les contacts sont dispersés, les relances arrivent trop tard, et on perd des opportunités “sans s’en rendre compte”.
        L’objectif d’UpStrategia : remettre de la clarté et une structure simple, puis automatiser seulement si ça a du sens.
      </p>

      <div class="mt-6 grid gap-4 sm:grid-cols-2">
        {principles.map((v) => (
          <div class="rounded-2xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-6">
            <h3 class="text-sm font-semibold text-[var(--brand-blue)]">{v.title}</h3>
            <p class="mt-3 text-sm text-[var(--text-muted)]">{v.text}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- METHODE -->
    <section class="py-12 sm:py-16">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Comment je travaille</h2>
      <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
        Une méthode simple : problème → solution simple → preuve → action.
      </p>

      <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {steps.map((s, idx) => (
          <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
            <div class="flex items-center gap-3">
              <span class="inline-flex h-8 w-8 items-center justify-center rounded-xl bg-black/5 text-sm font-semibold text-[var(--brand-blue)]">
                {idx + 1}
              </span>
              <h3 class="text-sm font-semibold text-[var(--brand-blue)]">{s.title}</h3>
            </div>
            <p class="mt-3 text-sm text-[var(--text-muted)]">{s.text}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- MINI PARCOURS (BREF) -->
    <section class="py-12 sm:py-16">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-white p-8 sm:p-10 shadow-sm">
        <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Mon parcours (en bref)</h2>
        <div class="mt-4 max-w-3xl space-y-4 text-sm leading-relaxed text-[var(--text-muted)]">
          <p>
            J’ai travaillé sur des sujets marketing & business en B2B, avec une obsession : relier la stratégie à l’exécution.
            Quand les bases (suivi, relances, contacts) sont claires, le marketing devient plus efficace — et surtout, tenable.
          </p>
          <p>
            Aujourd’hui, avec UpStrategia, j’accompagne des dirigeants qui veulent avancer sans se perdre dans des outils,
            des méthodes “trop théoriques” ou des automatisations inutiles.
          </p>
        </div>

        <div class="mt-7 flex flex-wrap gap-3">
          <a
            href="/contact?from=a-propos&diagnostic=1"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
          >
            Faire le point (15 min)
          </a>
          <a
            href="/offres"
            class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-[var(--bg-soft)] px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
          >
            Découvrir les offres
          </a>
        </div>
      </div>
    </section>

    <!-- GEO (utile SEO local) -->
    <section class="pb-16 pt-2">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-8 sm:p-10">
        <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">
          Basée à {geo.city} ({geo.region}) — interventions {geo.country}
        </h2>
        <p class="mt-3 max-w-3xl text-sm text-[var(--text-muted)]">
          La majorité des missions se fait en visio partout en France.
          Si vous êtes à {geo.city} et alentours, on peut aussi envisager du présentiel selon le projet.
        </p>
      </div>
    </section>

  </main>
</Layout>

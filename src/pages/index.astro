---
import Layout from "../layouts/Layout.astro";

const geo = { city: "Rennes", region: "Bretagne", country: "France" };

// Ce que vous gagnez (ultra concret)
const highlights = [
  {
    title: "Suivi clair",
    text: "Un seul endroit pour savoir qui relancer, quand, et pourquoi — fini les infos éparpillées.",
  },
  {
    title: "Relances simples",
    text: "Des rappels + des messages prêts à envoyer (sans spam, sans usine à gaz).",
  },
  {
    title: "Base propre",
    text: "Contacts nettoyés, structurés, exploitables : moins de doublons, plus d’opportunités.",
  },
];

// Symptômes (paroles de dirigeant)
const symptoms = [
  "Je relance quand j’y pense… donc trop tard",
  "Mes contacts sont dans Excel, les mails, le téléphone, LinkedIn…",
  "On ne sait pas où en est chaque opportunité",
  "J’ai déjà essayé un outil, c’était trop lourd",
  "Je veux quelque chose de simple, pas un nouveau chantier",
];

// 3 solutions = offres porte d’entrée (liens compatibles avec tes ancres actuelles)
const solutions = [
  {
    title: "CRM simple",
    text: "Centraliser vos contacts, savoir qui relancer et quand.",
    bullets: ["Contacts au même endroit", "Pipeline clair", "Routine simple"],
    href: "/offres/crm-simple",
    cta: "Découvrir le CRM simple",
  },
  {
    title: "Relances automatiques",
    text: "Des rappels et messages prêts à envoyer, sans spam.",
    bullets: ["Rappels simples", "Messages courts", "Suivi clair"],
    href: "/offres/relances-automatiques",
    cta: "Découvrir l’offre Relances",
  },
  {
    title: "Base de données propre",
    text: "Nettoyer et structurer vos contacts pour les exploiter.",
    bullets: ["Doublons supprimés", "Champs utiles", "Segmentation"],
    href: "/offres/base-donnees-propre",
    cta: "Découvrir l’offre Base",
  },
];

// Méthode (3 étapes max)
const methodSteps = [
  {
    title: "Point rapide",
    text: "15 minutes pour comprendre votre situation et vos priorités.",
  },
  {
    title: "Mise en place",
    text: "CRM + relances + base : simple, adapté à votre quotidien, documenté.",
  },
  {
    title: "Autonomie",
    text: "Mini-formation + routine claire pour que ça tienne dans le temps.",
  },
];

// Réassurance (sans promesse floue)
const reassurance = [
  {
    title: "Pas d’outil imposé",
    text: "On choisit la solution la plus simple pour vous (ou on simplifie l’existant).",
  },
  {
    title: "Minimum utile",
    text: "On garde uniquement les champs, étapes et automatismes qui servent vraiment.",
  },
  {
    title: "Documenté",
    text: "Vous repartez avec une doc courte + une routine simple (pas un projet qui dépend de moi).",
  },
];

const faq = [
  {
    q: "Je n’ai pas le temps de gérer un CRM.",
    a: "Justement : l’objectif est de vous faire gagner du temps. On met en place un CRM léger avec le minimum de champs, une routine simple, et des rappels pour éviter les oublis.",
  },
  {
    q: "Je suis sur Excel aujourd’hui, vous pouvez reprendre ma base ?",
    a: "Oui. Je nettoie et structure vos fichiers (doublons, champs utiles, segmentation) puis je les intègre dans le CRM choisi.",
  },
  {
    q: "Relances automatiques = spam ?",
    a: "Non. On parle de relances utiles et maîtrisées : rappels, messages courts, timing cohérent. Le but est d’être régulier, pas agressif.",
  },
  {
    q: "Vous travaillez uniquement à Rennes ?",
    a: `Je suis basée à ${geo.city} (${geo.region}). J’interviens principalement en visio partout en ${geo.country}, et sur site selon projet.`,
  },
  {
    q: "En combien de temps c’est en place ?",
    a: "Souvent en quelques jours à quelques semaines selon votre point de départ (qualité de la base, outil existant, volume). On vise du concret rapidement.",
  },
  {
    q: "Et l’IA dans tout ça ?",
    a: "Seulement si ça aide vraiment (ex : tri, synthèse, aide à la rédaction). Pas d’usine à gaz, pas de dépendance.",
  },
];

const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faq.map((x) => ({
    "@type": "Question",
    name: x.q,
    acceptedAnswer: { "@type": "Answer", text: x.a },
  })),
};
---

<Layout
  title={`CRM simple & relances automatiques pour TPE/PME B2B – ${geo.city} | UpStrategia`}
  description={`Mise en place de CRM simple, relances automatiques sans spam et nettoyage de base clients/prospects. Pour dirigeants de TPE/PME B2B. Basée à ${geo.city} (${geo.region}), visio ${geo.country}.`}
  canonical="/"
>
  {faqJsonLd && (
    <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />
  )}

  <main class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <!-- HERO -->
    <section class="py-12 sm:py-16">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-8 sm:p-12">
        <div class="grid gap-10 lg:grid-cols-[1.05fr_.95fr] lg:items-center">
          <div>
            <p class="text-xs uppercase tracking-wide text-[var(--text-muted)]">
              Conseil B2B — {geo.city} / {geo.region} — visio {geo.country}
            </p>

            <h1 class="mt-2 text-3xl sm:text-5xl font-semibold tracking-tight text-[var(--brand-blue)]">
              CRM simple + relances automatiques (sans spam) pour dirigeants de TPE/PME B2B à {geo.city}
            </h1>

            <p class="mt-4 text-base sm:text-lg text-[var(--text-muted)]">
              Vous avez des prospects et des clients… mais le suivi part dans tous les sens ?
              Je mets en place un <strong>CRM simple</strong>, des <strong>relances propres</strong> et une <strong>base de données claire</strong>
              pour que vos opportunités ne se perdent plus.
            </p>

            <div class="mt-7 flex flex-wrap gap-3">
              <a
                href="/contact?from=home&diagnostic=1"
                class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
              >
                Faire le point en 15 min (gratuit)
              </a>
              <a
                href="#solutions"
                class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-white px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
              >
                Voir les 3 solutions simples
              </a>
            </div>

            <ul class="mt-5 space-y-2 text-sm text-[var(--text-muted)]">
              <li>• Un seul endroit pour savoir qui relancer et quand</li>
              <li>• Moins d’oublis grâce à des rappels simples</li>
              <li>• Une base propre = des relances utiles + des décisions plus faciles</li>
            </ul>

            <p class="mt-4 text-xs text-[var(--text-muted)]">Réponse sous 48h en général. Basée à {geo.city}.</p>
          </div>

          <!-- PHOTO / RÉASSURANCE -->
          <aside class="rounded-3xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
            <div class="aspect-[4/5] overflow-hidden rounded-2xl bg-black/5">
              <img
                src="/images/carole-yche.PNG"
                alt="Carole Yché – UpStrategia"
                class="h-full w-full object-cover"
                loading="eager"
              />
            </div>

            <div class="mt-5">
              <p class="text-sm font-semibold text-[var(--brand-blue)]">Carole Yché</p>
              <p class="mt-1 text-sm text-[var(--text-muted)]">
                J’aide les dirigeants à remettre de l’ordre dans le suivi commercial : CRM simple, relances, base exploitable.
              </p>

              <div class="mt-4 flex flex-wrap gap-3">
                <a
                  href="/realisations"
                  class="inline-flex items-center gap-2 text-sm font-medium text-[var(--brand-copper)] hover:underline"
                >
                  Voir des exemples →
                </a>
                <a
                  href="/a-propos"
                  class="inline-flex items-center gap-2 text-sm font-medium text-[var(--text-muted)] hover:underline"
                >
                  Mon parcours →
                </a>
              </div>
            </div>
          </aside>
        </div>
      </div>
    </section>

    <!-- HIGHLIGHTS -->
    <section class="py-8 sm:py-10">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Ce que vous gagnez</h2>
      <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
        Du suivi clair, des relances régulières, et une base propre — sans outil compliqué.
      </p>

      <div class="mt-6 grid gap-4 sm:grid-cols-3">
        {highlights.map((h) => (
          <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
            <h3 class="text-sm font-semibold text-[var(--brand-blue)]">{h.title}</h3>
            <p class="mt-3 text-sm text-[var(--text-muted)]">{h.text}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- SYMPTOMS -->
    <section class="py-10 sm:py-12">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-white p-8 sm:p-10 shadow-sm">
        <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Vous vous reconnaissez ?</h2>
        <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
          Si vous cochez 2–3 cases, on a une vraie marge de progression rapidement.
        </p>

        <ul class="mt-6 grid gap-3 sm:grid-cols-2">
          {symptoms.map((x) => (
            <li class="rounded-2xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-4 text-sm text-[var(--text-muted)]">
              {x}
            </li>
          ))}
        </ul>

        <div class="mt-7 flex flex-wrap gap-3">
          <a
            href="/contact?from=home&diagnostic=1"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
          >
            Faire le point en 15 min
          </a>
          <a
            href="#solutions"
            class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-white px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
          >
            Voir les solutions
          </a>
        </div>
      </div>
    </section>

    <!-- SOLUTIONS -->
    <section id="solutions" class="py-12 sm:py-16">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">3 solutions simples</h2>
      <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
        CRM, relances, base de données : l’essentiel pour ne plus perdre d’opportunités.
      </p>

      <div class="mt-6 grid gap-4 lg:grid-cols-3">
        {solutions.map((s) => (
          <a
            href={s.href}
            class="group rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"
          >
            <h3 class="text-base font-semibold text-[var(--brand-blue)]">{s.title}</h3>
            <p class="mt-2 text-sm text-[var(--text-muted)]">{s.text}</p>

            <ul class="mt-4 space-y-2 text-sm text-[var(--text-muted)]">
              {s.bullets.map((b) => (
                <li>• {b}</li>
              ))}
            </ul>

            <div class="mt-5 inline-flex items-center gap-2 text-sm font-medium text-[var(--brand-copper)] group-hover:underline">
              {s.cta} →
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- METHOD -->
    <section class="py-12 sm:py-16">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Comment ça se passe</h2>
      <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
        Une méthode simple : on clarifie, on met en place, et vous êtes autonome.
      </p>

      <div class="mt-6 grid gap-4 sm:grid-cols-3">
        {methodSteps.map((s, idx) => (
          <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
            <div class="flex items-center gap-3">
              <span class="inline-flex h-8 w-8 items-center justify-center rounded-xl bg-black/5 text-sm font-semibold text-[var(--brand-blue)]">
                {idx + 1}
              </span>
              <h3 class="text-sm font-semibold text-[var(--brand-blue)]">{s.title}</h3>
            </div>
            <p class="mt-3 text-sm text-[var(--text-muted)]">{s.text}</p>
          </div>
        ))}
      </div>

      <div class="mt-8 rounded-3xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-8 sm:p-10">
        <h3 class="text-xl font-semibold text-[var(--brand-blue)]">Ce que je refuse (et ce que je fais à la place)</h3>
        <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
          Pas de jargon, pas d’outils complexes “par principe”, pas de promesses floues. On fait simple, utile, mesurable.
        </p>

        <div class="mt-6 grid gap-4 sm:grid-cols-3">
          {reassurance.map((r) => (
            <div class="rounded-2xl border border-[var(--border-soft)] bg-white p-6 shadow-sm">
              <h4 class="text-sm font-semibold text-[var(--brand-blue)]">{r.title}</h4>
              <p class="mt-3 text-sm text-[var(--text-muted)]">{r.text}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- GEO -->
    <section class="py-10 sm:py-12">
      <div class="rounded-3xl border border-[var(--border-soft)] bg-[var(--bg-soft)] p-8 sm:p-10">
        <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Basée à {geo.city} – {geo.region} (visio {geo.country})</h2>
        <p class="mt-3 max-w-3xl text-sm text-[var(--text-muted)]">
          J’accompagne principalement en visio partout en France. Si vous êtes à {geo.city} et alentours, on peut aussi envisager du présentiel selon le projet.
        </p>

        <div class="mt-6 flex flex-wrap gap-3">
          <a
            href="/contact?from=home&diagnostic=1"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
          >
            Prendre rendez-vous
          </a>
          <a
            href="/offres"
            class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-white px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
          >
            Voir les offres
          </a>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="pb-16 pt-6">
      <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">FAQ</h2>

      <div class="mt-6 space-y-3">
        {faq.map((item) => (
          <details class="rounded-2xl border border-[var(--border-soft)] bg-white p-5 shadow-sm">
            <summary class="cursor-pointer list-none text-sm font-medium text-[var(--text-main)]">
              {item.q}
              <span class="float-right text-[var(--text-muted)] transition">⌄</span>
            </summary>
            <p class="mt-3 text-sm leading-relaxed text-[var(--text-muted)]">{item.a}</p>
          </details>
        ))}
      </div>

      <div class="mt-10 rounded-3xl border border-[var(--border-soft)] bg-white p-8 sm:p-10 shadow-sm">
        <h2 class="text-2xl font-semibold text-[var(--brand-blue)]">Envie d’un suivi commercial clair ?</h2>
        <p class="mt-2 max-w-3xl text-sm text-[var(--text-muted)]">
          On fait le point en 15 minutes : votre situation, vos priorités, et la prochaine étape simple.
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a
            href="/contact?from=home&diagnostic=1"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-copper)] px-6 py-3 text-sm font-medium text-white hover:opacity-90 transition"
          >
            Faire le point en 15 min
          </a>
          <a
            href="#solutions"
            class="inline-flex items-center justify-center rounded-full border border-[var(--border-soft)] bg-[var(--bg-soft)] px-6 py-3 text-sm font-medium text-[var(--text-main)] hover:bg-black/5"
          >
            Voir les 3 solutions
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>
